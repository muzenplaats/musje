import { el } from '../utils/html'
import box from './box'

export default function scoreElement(scoreLayout) {

  return el.create('g', [
    // box(cellLayout, 'green'),
    // box(dataLayout, 'blue'),
  ])
}
