
Whiteboard

Ideas from musje 3

“Thinking about a declarative language in which the procedure language can be inside the component construct.”

Include a from ‘path/to/a’  #

Set {
  Identifier { a }
  Include { path/to/a }
}
Set { a = path/to/a }
Set a = Include { path/to/a }

# occurrence: (Schema thinking) ok
# a{m,n}  # in terms of aggression
#.              # m..n
#.               # Modeling (blueprints!) here
# shorthands
# a{n}  a{0,n} but not a{n,n}. 0..n
# a?  a{0,1}. 0..1
# a  a{1,1}. 1..1
# a* a{0,oo} 0..oo
# a+ a {1,oo}. 1..oo
# Another, enhance grammar by schema a := b*
a := b{0,2}

Set piA = PISession { … }  # * or +? * by definition

RootName {
  piA
  PISession { … }  # *
  piA
}

Set piB = PISession { … }


Set piA…
UnknownYet {
  piA
    RootName {}
  piB
}
Set piB…

WrapperName {
  RootName {}  # original name
}

Annotation { Put annotation here }
Description { so on }
Comment { comment }

—
SS := S* comment?
WS := (S | NL | comment)*
comment := ‘#’ All
All := /.*/
