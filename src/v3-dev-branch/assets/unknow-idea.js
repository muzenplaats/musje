
class Data {
  // Don't know; leave it for later...
}

class Component extends Data {
  constructor(src) {
    this.name = 'component'
  }

  toJSON() {
    cons { name } from this
    return { name }yeyoucanselike
  }
}
